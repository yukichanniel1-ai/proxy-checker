{
  "name": "proxy-checker-telegram-bot",
  "version": "1.0.0",
  "description": "Proxy Manager API — scrapes + checks + serves live proxies",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "start": "node proxy-all.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "socks-proxy-agent": "^8.0.0",
    "http-proxy-agent": "^7.0.0",
    "https-proxy-agent": "^7.0.0",
    "form-data": "^4.0.0"
  }
}
